/**
 * @param {string} s
 * @return {number}
 */
var calculate = function (s) {
    let arg1_A = [-1, -1];
    let arg2_A = [-1, -1];
    let add_b = null;
    for (let [i_n, state_n] = [0, 0]; i_n < s.length; ++i_n) {
        if(s.charAt(i_n)){}
    }
    //determine operation
};